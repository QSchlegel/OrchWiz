[local]
localhost ansible_connection=local infrastructure_kind=kind

# Optional:
# - set infrastructure_kind=minikube to use Minikube URL resolution
# - set kube_context_override=<context> via environment KUBE_CONTEXT if needed
